<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Parker's Tech Blog</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <header>
        <h1>Parker's Tech Blog</h1>
        <nav>
            <a href="/">Home</a>
            {{#if loggedIn}}
            <a href="/dashboard">Dashboard</a>
            <button id="logout">Logout</button>
            {{else}}
            <a href="/login">Login</a>
        </nav>
    </header>

    <main>
        {{{{ body }}}
    </main>

    {{#if loggedIn}}
    <script src=""></script>
    {{/if}}
</body>

</html>